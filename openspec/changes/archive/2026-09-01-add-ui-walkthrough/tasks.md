# Tasks: add-ui-walkthrough

## 1. Walkthrough

- [x] 1.1 `stages/walkthrough.py`: Walker protocol + PlaywrightWalker (lazy
  import, bounded visit) + journaling (observed evidence, ui_screenshot,
  ui_review via research-class call); skip-as-debt path; verify with a fake
  walker in offline tests
- [x] 1.2 BriefInputs.app_url + CLI `--app-url`; verify create path

## 2. Report

- [x] 2.1 Functional UI review section in page (with screenshots) and deck;
  verify presence with fixture session and absence without app_url

## 3. Definition of done

- [x] 3.1 `make check` green
