# Tasks: fix-test-artifact-selection

## 1. Fix

- [x] 1.1 Filter primary-artifact selection to prototype kinds; verify via the dojo e2e where the first journaled artifact is a panel manifest and the evaluator prompt must not contain manifest content
