# Tasks: add-init-wizard

## 1. Init

- [x] 1.1 Three brief templates in `cli/templates.py`; `bokken init` verb
  (interactive + `--template/--out`); Brief validation before write; next-
  command epilogue; tests for both paths
