# Tasks: showcase-demo-ui-tests

## 1. UI showcase

- [x] 1.1 Mock app fixture on the --lz tokens with the lying-tick defect;
  demo brief declares file:// app_url; DemoProvider scripts inventory,
  digest-parsed stepping, and the UI review; gallery builds with [ui];
  tests for both the exercised and the skipped path
