# Tasks: add-report-themes

## 1. Themes

- [x] 1.1 theme module (builtins + JSON loader + hex validation); page/deck/
  generate threading; --theme on export and new (journaled); tests incl.
  end-to-end themed regeneration
